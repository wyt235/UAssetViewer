using System.Windows;

namespace UAssetViewer;

/// <summary>
/// MainWindow — 主窗口 code-behind
/// DataContext 在 XAML 中绑定到 MainViewModel
/// </summary>
public partial class MainWindow : Window
{
    public MainWindow()
    {
        InitializeComponent();
    }
}